{
  "name": "nl-offline",
  "version": "0.22.0",
  "private": true,
  "description": "Offline Newfoundland and Labrador town and road navigation PWA",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:integrity": "node --test tests/package-integrity.test.mjs",
    "test:routing": "node --test tests/route-regression.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
