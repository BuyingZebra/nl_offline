{
  "name": "nl-offline",
  "version": "0.20.0",
  "private": true,
  "description": "Offline Newfoundland and Labrador map and local routing PWA",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:integrity": "node --test tests/package-integrity.test.mjs",
    "test:routing": "node --test tests/route-regression.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
